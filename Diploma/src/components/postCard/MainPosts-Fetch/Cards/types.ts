export interface ICard {
  id: number;
  image?: string;
  lesson_num: number;
  title: string;
  author: number;
}

export interface CardProps {
  id: string;
  image?: string;
  title: string;
  year: string,
  
  
  
}
